<div class="container mt-5">
  <div class="card shadow-sm">
    <div class="card-body">
      <h1 class="mb-3"><%= @task.title %></h1>
      <p class="bg-light p-3 rounded border"><%= @task.details %></p>

      <div class="form-check mb-4">
        <%= check_box_tag "completed_#{@task.id}", '1', @task.completed %>
        <%= label_tag "completed_#{@task.id}", "Completed" %>
      </div>

      <div class="d-flex flex-wrap gap-2">
        <%= link_to "✎", edit_task_path(@task), class: "btn btn-outline-primary" %>
        <%= link_to "🗑 Delete",
          task_path(@task),
          data: {turbo_method: :delete, turbo_confirm: "Are you sure?"},
          class: "btn btn-outline-danger" %>
        <%= link_to "Back to tasks", tasks_path, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>
</div>
